deployment:
  id: "my_string_cat_8_thread_dep"
  description: "some random deployment"
  autoscale_config:
    min_replicas: 0
    max_replicas: 1
    traffic_history_seconds: 100
    scale_down_delay_seconds: 30
    scale_up_delay_seconds: 30
    enable_packing: true
  worker:
    model:
      id: "gender-demographics-recognition"
      model_version:
        id: "ff83d5baac004aafbe6b372ffa6f8227"
      user_id: "clarifai"
      app_id: "main"
  scheduling_choice: 4
  nodepools:
    - id: "test-nodepool-6"
      compute_cluster:
          id: "test-aws-cluster"
