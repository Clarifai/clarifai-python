# This is the sample config file for a MCP model.

model:
  id: "dummy-runner-model"
  user_id: "user_id"
  app_id: "app_id"
  model_type_id: "mcp"

build_info:
  python_version: "3.12"

inference_compute_info:
  cpu_limit: "1"
  cpu_memory: "1Gi"
  num_accelerators: 0
